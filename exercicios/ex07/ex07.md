### 7 - Write a python class called PinholeCamera, the class should have the following methods:

1) Constructor
```python
def __init__(self, K : np.ndarray ,dist_coefs : np.ndarray, world_R_cam : np.ndarray  , world_t_cam : np.ndarray )
  """
  constructor, that takes camera intrinsic and extrinsics
  """
```

2) Project
```python
def project(self, world_pts_3d : np.ndarray) -> np.ndarray
  """
    projects a 3d array of points into camera plane
  """
```

3) Unproject
```python
def project(self, image_pts_2d : np.ndarray) -> np.ndarray
  """
    projects 2d detected points from the image as camera rays
  """
```

write tests (with data that you create) that show that your class  works properly

Basicamente teremos uma classe chamada PinholeCamara, com duas funções principais: 
- Project, que vai pegar os pontos 3D do mundo e descobrir onde eles aparecem na imagem
- Unproject, que vai pegar pontos 2D da imagem e transformar em raios saindo da câmera
